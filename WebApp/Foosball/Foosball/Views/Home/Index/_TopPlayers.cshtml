<div class="x_panel">
    <div class="x_title">
        <h2>Player Leaderboard</h2>
        <span class="pull-right">
            <a asp-area="" asp-controller="Players" asp-action="Index">
                (All Players)
            </a>
        </span>
        <div class="clearfix"></div>
    </div>
            
    <ul class="list-unstyled top_profiles scroll-view">
        @{int count = 0;}
        @foreach (var rating in ViewBag.TopPlayerRatings)
        {
            if (rating.Player != null)
            {
                count++;
                <li class="media event">
                    <a class="pull-left border-aero profile_thumb">
                        <i class="fa fa-user aero"></i>
                    </a>
                    <div class="media-body">
                        <a class="title" href="#">@count.</a>
                        <p>
                            <a asp-area="" asp-controller="Players" asp-action="Profile" asp-route-id="@rating.Player.Id">
                                <strong>@rating.Player.Name</strong>
                            </a>
                        </p>
                        <p>
                            <span class="label label-default">@rating.ELO</span>
                            <span class="pull-right"><small>@rating.LastUpdatedAt</small></span>
                        </p>
                    </div>
                </li>
            }
        }
    </ul>
</div>